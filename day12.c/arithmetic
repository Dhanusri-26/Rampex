#include <stdio.h>

int main() {
    char str[] = "Hello, World!";
    char *ptr = str;
    int length = 0;

    // Traverse the string using pointer arithmetic
    while (*ptr != '\0') {
        length++;
        ptr++;
    }

    printf("Length of the string: %d\n", length);
    return 0;
}
