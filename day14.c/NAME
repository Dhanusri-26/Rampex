#include <stdio.h>

int main() {
    char name[50];  // Declare a character array to store the name

    // Prompt the user to enter their name
    printf("HELLO ");
    printf("DHANUSRI SAKTHIVEL %s!\n", name);

    return 0;
}
